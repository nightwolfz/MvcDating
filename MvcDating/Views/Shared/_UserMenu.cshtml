@if (Request.IsAuthenticated)
{
    @Html.ActionLink(User.Identity.Name, "Index", "Profile", new {username = User.Identity.Name}, new {@class = "navbar-brand", title = "My Profile"})
}
else
{
    @Html.ActionLink("MvcDating", "Index", "Home", null, new {@class = "navbar-brand"})
}